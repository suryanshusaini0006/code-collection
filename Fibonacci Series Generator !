#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int i,n,a=0,b=1,sum=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++){
 printf("%d ",a);
        sum=a+b;
        a=b;
        b=sum;
    
    
    }

    return 0;
}
