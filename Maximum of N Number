#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

int i,n,num,max=0;
    scanf("%d",&n);
    scanf("%d",&max);
    for(i=1;i<=n;i++){
        scanf("%d",&num);
        if(num>max)
            max=num;
        }
    printf("%d",max);
    return 0;
}
