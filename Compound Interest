#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

int P;
float R;
    int T;
    float CI;
    scanf("%d\n%f\n%d\n",&P,&R,&T);

    
CI = P * (pow((1 + R/100), T) - 1);
    printf("%.2f\n",CI);

  
    return 0;
}
